#!/usr/bin/env bash
# This script is displaying "Best School" 10 times
END=10
n=1
while [[ $n -le $END ]]; do
	if (("$n" != "9")); then
		echo "Best School"
	else
		echo "Hi"
	fi

	((n = n + 1))
done
